export default defineAppConfig({
    ui: {
        colors: {
            primary: 'trouptrip-accent',    /* Nuxt UI primary = Ton accent */
            secondary: 'trouptrip-secondary', /* Nuxt UI secondary = Ton violet */
            error: 'trouptrip-error',        /* Nuxt UI error = Ton rouge */
            neutral: 'trouptrip-neutral'                  /* Obligatoire pour les gris de structure */
        }
    }
})