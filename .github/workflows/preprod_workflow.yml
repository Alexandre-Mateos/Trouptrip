name: Deploy Staging

on:
  push:
    branches: [ "staging" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_IP }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # 1. Aller dans le dossier du projet
            cd /opt/apps/trouptrip/preprod
            
            # 2. Récupérer les derniers changements
            git fetch origin staging
            git reset --hard origin/staging
            git clean -fd
            
            # 3. Re-construire l'image localement et relancer le conteneur
            docker compose -f docker-compose.yaml -f docker-compose.prod.yaml up -d --build
            
            docker compose exec -T php php bin/console doctrine:database:create --if-not-exists
            docker compose exec -T php php bin/console d:m:m --no-interaction
            docker compose exec -T php php bin/console lexik:jwt:generate-keypair --skip-if-exists
            
            # 4. Nettoyage des images inutilisées
            docker image prune -f