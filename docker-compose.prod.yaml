services:
  php:
    restart: always
    environment:
      APP_ENV: prod

  nginx-service:
    restart: always
    networks:
      - default
      - web

  db:
    restart: always

  node-front:
    restart: always
    networks:
      - default
      - web

networks:
  web:
    external: true