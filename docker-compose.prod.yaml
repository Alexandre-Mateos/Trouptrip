services:
  php:
    restart: always
    env_file:
      - .env

  nginx-service:
    restart: always
    networks:
      - default
      - web

  db:
    restart: always

  node-front:
    restart: always
    networks:
      - default
      - web

networks:
  web:
    external: true