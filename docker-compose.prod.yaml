services:
  php:
    restart: always
    environment:
      APP_ENV: prod

  nginx:
    restart: always
    networks:
      - default
      - web

  db:
    restart: always

  node-front:
    restart: always
    networks:
      - default
      - web

networks:
  web:
    external: true